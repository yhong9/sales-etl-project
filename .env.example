POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_password
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=olist_warehouse
# Set to require for hosted PostgreSQL providers such as Neon.
# Leave empty for a local PostgreSQL server that does not use SSL.
POSTGRES_SSLMODE=
